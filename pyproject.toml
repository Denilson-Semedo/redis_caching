[tool.poetry]
name = "redis-caching"
version = "0.1.0"
description = ""
authors = ["Denilson Tavares <ext-d.tavares@decsis.pt>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
redis = "^5.0.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
